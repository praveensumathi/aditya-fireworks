function SectionTitle({ title }) {
  return (
    <h1
      className={`font-extrabold text-center items-center m-autom text-4xl text-primary`}
    >
      {title}
    </h1>
  );
}

export default SectionTitle;
